import React, { Component } from 'react';

import './Stats.css';

const stats = (props) => {
    return (
        <div 
        className={"Stats"}
   //     name={props.name}       <h2>{name}</h2>
        >
    
            
        </div>
    )
};

export default stats;